# Copy to .env for local development. Not needed when deployed as a
# Databricks App — DATABRICKS_HOST/auth come from the app's own identity there,
# and DATABRICKS_WAREHOUSE_ID is set in app.yaml instead.

DATABRICKS_HOST=https://dbc-fa603402-4338.cloud.databricks.com
DATABRICKS_TOKEN=
DATABRICKS_WAREHOUSE_ID=22c7742226f54c2f

# Optional. Trailing-days window for /api/pipelines/overview's KPIs.
OVERVIEW_WINDOW_DAYS=7
