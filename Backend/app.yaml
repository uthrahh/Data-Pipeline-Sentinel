command:
  - "uvicorn"
  - "app:app"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "8000"

env:
  - name: "DATABRICKS_WAREHOUSE_ID"
    value: "22c7742226f54c2f"
  # DATABRICKS_HOST / auth are provided automatically by the app's own
  # service-principal identity when running as a Databricks App — do not set
  # DATABRICKS_TOKEN here.
